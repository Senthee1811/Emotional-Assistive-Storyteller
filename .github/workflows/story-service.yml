name: Story Service CI

on:
  push:
    paths:
      - 'services/story-service/**'
      - '.github/workflows/story-service.yml'
  pull_request:
    paths:
      - 'services/story-service/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: cd services/story-service && pip install -r requirements.txt
      - name: Build Docker Image
        run: docker build -t story-service:latest ./services/story-service
